# Example environment variables for estimate_backend

# Database (async SQLite default is used if not set)
# DATABASE_URL=sqlite+aiosqlite:///./data.db
# DB_ECHO=false

# Default hourly rate fallback if settings table is not set
HOURLY_RATE=120
