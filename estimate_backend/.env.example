# Database configuration
# Use an async SQLAlchemy URL. Defaults to sqlite+aiosqlite:///./data.db when unset.
DATABASE_URL=sqlite+aiosqlite:///./data.db
# Echo SQL statements (true/false). Defaults to false.
DB_ECHO=false
